# ✅ eye 实测检查清单

> 在 DSH Web 环境中安装后按序验证。预计 2-3 分钟。
> 会话级安装:把 `host.js` / `client.js` 内容喂给 `cordis_define`(`idPrefix: "eye"`),`cordis_run` 激活并批准授权。

## 0. 前置

- [ ] DSH Web 已运行,当前模型为纯文本模型(如 `deepseek-official / deepseek-v4-flash`)
- [ ] 准备 1 张含文字的图片(截图即可)+ 1 张纯照片(无文字)

## 1. 插件加载

- [ ] `cordis_run` 激活后,**侧边栏底部**出现「👁 诊断 / 切换到eye」按钮
- [ ] 点「👁 诊断」,文本显示 `当前=<provider/model> | 组=...` 且**不报错**
- [ ] 设置页出现「eye 视觉桥」区块

## 2. 配置与测试连接(会话版)

- [ ] 设置页「预置提供商」选「智谱 GLM-4V-Flash(免费)」→ URL 与模型名自动填充
- [ ] 填入 API Key,点「测试连接」→ 显示 `连接成功 ✓ 延迟 <N>ms,模型回复:...`(绿字)
- [ ] 填错 Key 再点测试 → 显示红字错误信息(**不应**卡死或空白)
- [ ] 点「保存」→ 显示「已保存 ✓」

## 3. 拖图即读(核心链路)

- [ ] 点击「切换到eye」→ 提示 `已切换到 eye-vision/<模型> ✓`
- [ ] 在会话中**拖入文字截图**并发送 → 模型回答能正确引用图中文字
- [ ] 换一张**纯照片**发送 → 模型能描述画面内容(此时走 VLM 语义描述;无 VLM 时提示 OCR 未识别到文字也属正常)

## 4. 缓存与多图

- [ ] **同一张图 + 同问题**重发 → 第二次明显更快(命中缓存,无第二次 OCR/VLM 调用)
- [ ] **一次发 2 张图** → 模型能对比两张图(多图合并到一次 VLM)

## 5. eye_see 工具

- [ ] 让模型调用 `eye_see`,`file_path` 指向本地图片绝对路径 → 返回 OCR+VLM 文本
- [ ] `mode=ocr`(仅 OCR)/ `mode=vlm`(仅 VLM)分别验证
- [ ] `file_path` 传一个 http(s) 图片 URL(如 `https://.../a.png`)→ 能下载并识别(VLM 路径需已配置)

## 6. 边界与回滚

- [ ] 切回纯文本模型(开新会话,无图片历史)→ 正常对话不受影响
- [ ] 图片会话切回纯文本模型 → 预期被拒(`model-unavailable: this session already contains images`),属已知行为

## 常见失败排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 按钮/设置页不出现 | 插件未激活或 client 半区未授权 | 重新 `cordis_run`;在界面批准授权 |
| 拖图后回复仍是"无法查看图片" | 图片块未被拦截 | 确认模型确为 eye-vision;检查 `.eye/eye.config.json` 是否生成 |
| 测试连接报 `HTTP 4xx` | URL 填错 / Key 无效 / 模型名不对 | 核对预置模板值与 Key |
| 测试连接报超时 | 网络代理/HTTPS_PROXY 污染 | 参考工作区 DEV-NOTES:先排除 `HTTPS_PROXY=127.0.0.1:10811` 干扰 |
| OCR 提示"未识别到文字" | 图片纯图形无文字 | 属正常;配置 VLM 后走语义描述 |
| 重启后插件消失 | 会话级特性 | 重装一次(2 分钟)或改用 host-native 永久版 |
